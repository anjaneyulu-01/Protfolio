# AI Portfolio

A minimal Vite + React portfolio starter.

Quick start (Windows PowerShell):

```powershell
cd d:/AI-portfilio
npm install
npm run dev
```

Notes:
- Add your avatar to `public/memoji.png` and the app will load it from `/memoji.png`.
- If the image fails to load, the app falls back to an embedded SVG avatar.
